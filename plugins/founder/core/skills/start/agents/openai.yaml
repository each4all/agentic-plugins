interface:
  display_name: "Founder — Start"
  short_description: "Sequence a business deliverable end-to-end: investigate → frame → decide → compose → critique → refine with approval gates"
  default_prompt: "Use $founder:start <business topic> to take one deliverable from discovery to a reviewed plan in one pass; for a multi-deliverable program use the orchestrator persona"

policy:
  allow_implicit_invocation: false
