interface:
  display_name: "Engineer — Investigate"
  short_description: "Gather evidence, inspect context, scan codebase, diagnose bugs, produce cited briefs"
  default_prompt: "Use $engineer:investigate to gather evidence on a topic, diagnose a bug, or produce a durable cited brief from external sources"

policy:
  allow_implicit_invocation: false
