interface:
  display_name: "Runtime - Context"
  short_description: "Context summary, explicit budget risk, artifact pointers, and next-session handoff scaffold"
  default_prompt: "Use $runtime:context to write a bounded context artifact or run a read-only explicit budget check without mutating host context."

policy:
  allow_implicit_invocation: false
