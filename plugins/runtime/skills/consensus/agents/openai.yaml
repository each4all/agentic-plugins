interface:
  display_name: "Runtime - Consensus"
  short_description: "Peer fanout, explicit companion execution, and consensus synthesis artifacts"
  default_prompt: "Use $runtime:consensus to plan peer fanout, execute companions only with execute --execute, record raw output as artifacts, and synthesize summary/disagreement evidence pointers."

policy:
  allow_implicit_invocation: false
