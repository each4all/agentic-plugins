interface:
  display_name: "Runtime - Consensus"
  short_description: "Artifact-only peer fanout and consensus synthesis scaffold"
  default_prompt: "Use $runtime:consensus to plan peer fanout, record raw output as artifacts, and synthesize summary/disagreement evidence pointers without executing peers."

policy:
  allow_implicit_invocation: false
