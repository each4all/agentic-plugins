interface:
  display_name: "Runtime - Doctor"
  short_description: "Read-only host readiness, companion, model/effort, and ledger diagnostics"
  default_prompt: "Use $runtime:doctor to inspect Claude/Codex availability, auth, agentic-plugins install/cache state, companion readiness, model/effort observation, and workflow ledger health without mutating settings."

policy:
  allow_implicit_invocation: false
