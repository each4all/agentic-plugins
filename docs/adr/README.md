# Architecture Decision Records (ADRs)

This directory contains the foundational architectural decisions for
agentic-plugins. Each ADR captures one decision with its context, the chosen
direction, and the consequences.

## Format

ADRs follow the standard 5-section format:

1. **Status** — `Proposed`, `Accepted`, `Superseded by ADR-NNNN`, `Deprecated`
2. **Context** — Forces at play and why a decision is needed
3. **Decision** — What was decided
4. **Consequences** — What follows from the decision (positive, negative, neutral)
5. **Alternatives Considered** — What else was on the table and why rejected

See [`template.md`](template.md) for the canonical layout.

## File naming

`NNNN-<kebab-case-slug>.md` where `NNNN` is a zero-padded sequence
number starting at 0001. Numbers are never reused. To replace a
decision, write a new ADR with `Status: Accepted` that references the
old one, and change the old one's status to `Superseded by ADR-NNNN`.

## Process

1. Copy `template.md` to `NNNN-<slug>.md` (next number)
2. Status starts as `Proposed`
3. Discuss / iterate
4. Merge with `Status: Accepted`
5. To supersede: new ADR + update old ADR's status

### Amendments vs Supersedes

When an Accepted ADR needs revision, the choice between *Amendment*
(adding to the existing ADR) and *Supersede* (writing a new ADR
that replaces all or part of the old one) follows a single
discriminator:

- **Amendment** — the original Decision-section prose remains
  *operatively accurate* after the change. The Amendment adds
  clarifications, sub-finding additions, or downstream cascades
  that follow from the original Decision. Pattern: ADR-0008's
  Amendments (additive clarifications), ADR-0010's 2026-05-06
  Amendment (downstream cascade from ADR-0014/0015).
- **Supersede** — the original Decision-section prose is
  *no longer operatively accurate*. A reader landing on the old
  ADR must be pointed at the new one for the operative decision.
  Pattern: ADR-0015 supersedes ADR-0014's timeline portion;
  ADR-0014's Decision §1 ("plugin remains installable through
  Stage 3 entry") is reversed.

Partial supersedure is supported: if an ADR's Decision sections
divide cleanly (e.g., capability decision vs timeline), the old
ADR's Status becomes `Superseded by ADR-NNNN (X portion only)` and
the new ADR scopes its supersedure to that portion. ADR-0014/0015
is the precedent.

When in doubt, ask: *does the original Decision-section prose remain
operatively accurate?* If no, write a new ADR (Supersede).

## Index

| # | Title | Status |
|---|---|---|
| [0001](0001-hexagonal-architecture.md) | Hexagonal architecture (core / adapter / companion) | Accepted |
| [0002](0002-adapter-contract.md) | Adapter contract — 4 required items | Accepted |
| [0003](0003-mcp-vs-companion.md) | MCP vs companion-CLI — when to use which | Accepted |
| [0004](0004-companion-ownership.md) | Companion ownership — agentic-plugins owns both | Accepted |
| [0005](0005-separate-repo-from-omcc.md) | Separate repo from omcc | Accepted |
| [0006](0006-directory-layout-install-pattern.md) | Directory layout + install pattern | Accepted |
| [0007](0007-migration-cutover-plan.md) | Migration cutover plan from omcc to agentic-plugins | Accepted |
| [0008](0008-companion-distribution-model.md) | Companion distribution model — `companions` plugin + cache-glob discovery + env override | Accepted |
| [0009](0009-companion-contract-v0-1-1-prompt-file-stdin-precedence.md) | Companion contract v0.1.1 — `--prompt-file` and `PROMPT_ARG` precedence over stdin | Accepted |
| [0010](0010-plugin-boundary-policy.md) | Plugin boundary policy — 4-layer composition + universal cognitive verbs + naming convention | Accepted |
| [0011](0011-workflow-continuity-storage.md) | Workflow continuity storage — minimal Option III for Stage 2 | Accepted |
| [0012](0012-omcc-removal-preconditions.md) | omcc + codex-plugin-cc removal preconditions | Accepted |
| 0013 | Codex CLI commands integration mechanism (file pending — Stage 3+ trigger) | Reserved |
| [0014](0014-plugins-research-deprecation.md) | plugins/research deprecation — capability folded into engineer:investigate cited-brief profile | Superseded by [ADR-0015](0015-research-archive-timeline-collapse.md) (timeline portion only) |
| [0015](0015-research-archive-timeline-collapse.md) | Research archive timeline collapse — supersedes ADR-0014 timeline portion | Accepted |
| [0016](0016-cross-package-commit-splitting.md) | Cross-package commit splitting for release-please routing | Accepted |
| [0017](0017-stage25-continuity-and-schema-roadmap.md) | Stage 2.5+ continuity and schema roadmap (meta commands + `ensemble_results` frontmatter + Stop auto-archive) | Accepted |
| [0018](0018-stage3-architecture-orchestrator-and-branch-context.md) | Stage 3+ architecture — orchestration capability + branch-as-workflow-context + cross-host verification | Accepted |
| [0019](0019-cross-plugin-invocation-contract.md) | Cross-plugin invocation contract — orchestrator → engineer (Stage 3+ §sub-1 follow-up) | Accepted |
| [0020](0020-engineer-integrated-workflow-umbrella.md) | engineer integrated workflow umbrella — `/engineer:start` lifecycle macro command | Accepted |
| [0021](0021-codex-command-surface-parity-via-skill-wrappers.md) | Codex command-surface parity via skill wrappers — macro skill category alongside verb skills | Accepted |
| [0022](0022-engineer-meta-skill-category.md) | engineer meta-skill category — third category in `skills/<plugin>/` for workflow-continuity ops | Accepted |
| [0023](0023-peer-runner-supervisor-layer.md) | Peer-runner supervisor layer for companion dispatch | Accepted |
| [0024](0024-runtime-operator-control-plane.md) | Runtime operator control plane — doctor/settings, dynamic consensus, context hygiene, and host readiness | Accepted |
| [0025](0025-workflow-storage-migration.md) | Workflow storage migration to `.agentic-plugins` | Accepted |
| [0026](0026-runtime-compatibility-drift-and-release-notes.md) | Runtime compatibility drift and release-note evidence | Accepted |
| [0027](0027-decide-skill-multi-axis-evolution.md) | Decide skill multi-axis evolution — axis registry, sizing, weighting/sensitivity, and the parallel-edit contract | Accepted |
| [0028](0028-engineer-phase7-commit-automation.md) | Engineer Phase 7 commit automation — 3-layer defense + 14 policy sections + centralized validate-commit.mjs | Accepted |
| [0029](0029-entry-routing-contract-enforcement.md) | Extend entry-routing contract enforcement to standalone verbs — active, evidence-based next-action guidance | Accepted |
| [0030](0030-codex-plugin-hooks-removal-stage-aware-migration.md) | Codex `plugin_hooks` removal — stage-aware runtime migration | Accepted |
| [0031](0031-session-level-active-handoff-layer.md) | Session-level active handoff layer — projection model composing context risk + workflow state + routing into a continue-vs-fresh decision | Accepted |
| [0032](0032-codex-per-plugin-command-surface-adoption.md) | Codex per-plugin command surface adoption — stage-aware runtime recognition | Accepted |
| [0033](0033-ci-full-test-suite-coverage.md) | CI full-suite coverage via test discovery — no-arg `npm test`, unfiltered full-tests gate, smoke namespace split, structural guard | Accepted |
| [0034](0034-codex-plugin-list-read-signal.md) | Codex `plugin list --json` as host-native installed-state read signal — list-authoritative-then-cache precedence, shared resolver, doctor-scoped (cross-script deferred) | Accepted |
| [0035](0035-runtime-active-execution-boundary-policy.md) | Runtime active-execution boundary policy — read-only default, R0/M1/H2/H3 mutation tiers (no open-ended higher tier), per-executor invariants, testable `MUST NOT` ceiling (registry+static-test enforcement planned), add-gate; Codex `codex plugin add` (C) as H2 worked example, `--apply-codex-plugin-hooks` flagged obsolete/non-conformant | Accepted |
| [0036](0036-founder-persona-business-planning.md) | founder persona (L3) — new-business planning workbench: business profiles/axes/taxonomy, git-substrate lifecycle, staged decision-L2 seam | Accepted |
| [0037](0037-image-capability-plugin.md) | `image` capability plugin (L2) — cross-host image generation via Codex's **integrated** gpt-image (native + `codex-companion` bridge, shared-fs artifact, text pointer); full six-verb, domains-as-profiles, prompt-mediated params (direct OpenAI API out of scope); applies ADR-0010 §6 Trigger 2 + §7 | Accepted |
| [0038](0038-runtime-permission-prompt-advisory.md) | Runtime permission-prompt reduction advisory — A (doctor R0 diagnosis + settings M1 dry-run plan) + C (usage-grounded recommendations, sanitized evidence) base, application stays with the user (no host-config write, §4 ceiling preserved); Guard Hook (B) **out of scope** — deferred to a separate effect-based ADR that must amend ADR-0035 §4 (shipping a permission-relaxing component is forbidden "independent of any flag"; flagged by adversarial cross-host review) | Accepted |
| [0039](0039-completion-footer-activation.md) | Completion/handoff footer activation — wire the orphaned `footer.mjs` render engine into the already-code-spoken ADR-0031 sidecar terminal path via **subprocess piggyback** (not a `/runtime:footer` command), promoting completion elements 2/3/4/7/8 from prose to code on engineer + orchestrator; `--context-state` (not `--risk`), stderr/file-only, `execFile` capture→stderr, `emitted===true` gate, single-emission guard, `discoverRuntimePluginRoot` copy-not-import, fail-closed silent; orchestrator fixed-literal removal + ADR-0029 shape as a separate deliverable; founder **deferred** with an onboarding recipe (both decisive 9-axis + independent cross-host peer converged) | Accepted |
| [0040](0040-operator-observability.md) | Operator situational awareness — (A) attention notifications: runtime-owned emitter core (shared event schema, source-excluded dedupe key + kind/subject mapping contract, atomic TTL claims; **built-in allowlisted channels** `none`/`macos-osascript` (`on run argv`, payload never in AppleScript source)/`file-log`; ADR-0035 §4 ceiling untouched — exercises §2's new-ADR executor path, classifying fixed-argv dispatch + notify-state retention as a bounded M1 extension w/ an explicit narrow §3-invariant amendment scoped to the emit executor; custom-argv channel deferred to a future §4-amending decide-by-effect ADR per 0038 §6) + tiny **hook-only L1** `plugins/attention` (ADR-0010 §6 Trigger 2; ADR-0008-style shape formalization) Claude sensors (`Notification` w/ `notification_type` matcher, `Stop`, `SubagentStop`; freshness-checked sidecar enrichment) + Codex via M1 fragment plans (`notify=` source-verified turn-complete-only single-key, stable receiver-shuttle constraint; approval attention via `tui.notifications`; `PermissionRequest` hook deferred w/ trigger) + peer-run terminal self-sensor ×3 persona peer-runners (incl. orchestrator pre-ledger path; `pruned` skipped); (B) `runtime:dashboard` Tier 1+2 — 3-persona state (founder via direct namespace scan, doctor contract untouched) + operator health (pull-side) on extracted doctor readers; snapshot + bounded `--watch`; fail-closed silent, runtime stays hook-free (this ADR's decision) | Accepted |
| [0041](0041-cross-machine-notification-egress.md) | Cross-machine notification egress — **amends ADR-0035 §4 head-on** to add exactly one new effect domain, **tier E1 (enumerated-metadata network egress)**: a `notify.mjs` channel POSTs a *redacted, enumerated, capped* metadata field set (`kind`/`hostname`/`topic`/`session_hint`/`workflow_id`/`phase` — never title/body/message/next_action/transcript/raw text) to a **fixed SaaS host** (v1 Telegram), behind an **env / verified-ignored-local** opt-in with an **env-only** credential (a token alone never activates; tracked/repo config can never activate; HOME-is-repo inside-repo files fail-closed), over a **fully-pinned no-redirect** `fetch` (curl not registered; the one in-process fetch is registry/AST-scanned + permitted only in notify.mjs — the keystone gate lands before any fetch use), with a separate `buildEgressPayload` (enumerated-only, provably excludes event_id), egress-only **secret-scrub (scrub-before-cap)**, an attempt-**mirror** to file-log (`egress_channel`/`egress_status`, sanitized), and **claim-finalization** dedupe (a failed dispatch RELEASES the claim so a config fix re-fires; a repeated failure ENGAGES a fingerprint-keyed backoff throttle; a config fix BYPASSES it). One channel serves **both** hosts (Claude attention sensors + Codex `notify=` shuttle); `hostname` weaves into the `event_id` so all machines × all sessions fan into **one chat** while staying distinct. Owner-decision gate: **Accept** (bounded §4 amendment, not a general egress precedent) vs Forbid→Alt A; recurring multi-machine demand confirmed → accept live | Accepted |
| [0042](0042-designer-persona-design-ux-workbench.md) | `designer` persona (L3) — code-first design/UX decision & quality workbench: pre-code design (user flow / wireframe spec / CTA copy / IA) + **post-code quality critique loop** (heuristic/a11y/conversion/consistency lenses over screenshot vision + frontend code), 7-axis decision pool (`usability` common decisive + `accessibility` common veto encoded `role: supporting`+`gate: true`, ADR-0027 enum unchanged), 4 presets mapped by 5 L4 profiles (general+flow → balanced); **Figma excluded** (code-first, cross-host symmetric) / `image` L2 **composed not re-implemented** (ADR-0037); non-dispatch (founder precedent), copy-not-import machinery (ADR-0029); applies ADR-0010 §6 Trigger 3 + §7; drafted via `engineer:start` + Codex cross-host peer review (verdict=block, 10 findings applied); Accepted 2026-07-09 after the PR7 real-topic dogfood (7 defects found, 6 fixed, 1 recorded as demand-gated follow-up); cascades ADR-0010 §1 L4 (drops `image`/`print`/`brand`/`motion`/`frontend` from designer profiles) + §7 (third L3 occupant) | Accepted |
| [0043](0043-founder-designer-footer-enablement.md) | founder/designer workflow-projection + completion-footer enablement — the runtime-enablement ADR that ADR-0042 required and ADR-0039 §7 / ADR-0031 Amendment §Scope deferred to: `VALID_WORKFLOW_KINDS` widens to the four personas (unsupported-kind degradation path **stays**, regression re-pinned on a genuinely unknown kind + fixture-conversion sweep; `PLUGIN_COMMAND_RE` designer omission fixed on the same surface); founder/designer onboard by the ADR-0039 §7 recipe under the ADR-0031 Amendment's eight decisions with an **explicit behavioral baseline** (engineer's path-targeted projection + orchestrator's hardened delivery/stale-clear; `emitHandoff` opt-in; last-writer-wins slot accepted; orphan-sweep path honestly out of promise; copy-not-import per ADR-0010 §5); completion-flag mapping comes from the **S9 output contract**, not engineer's blocked-collapse; footer-rendered marker = **documented cross-package contract** (attention hardcodes filename+JSON gate); dual footer/notify discovery floors with capability-file parameterization, **footer floor = first released runtime version containing the enum expansion** (never pinned pre-release); rollout `(S2→release→install) ∥ S9 → S3∥S4 → install + Codex /hooks re-attestation`, S2 non-revertible after personas ship (personas-first rollback + one-shot artifact cleanup), per-package commits (ADR-0016); attention `SENSOR_PERSONAS` expansion deferred w/ trigger (S3/S4 released), dashboard designer Tier-1 inclusion and doctor ledger widening out of scope (orthogonal per-surface decisions — founder proves it) | Accepted |
| [0044](0044-session-generic-handoff-capture.md) | Session-generic handoff capture — code firing point for out-of-workflow session boundaries (honestly a **rolling turn-complete checkpoint**): two-tier trust split (guaranteed machine-derived structural floor with observer-labeled fields + opportunistic staged note whose provenance proves the **channel, not the author**); fired by attention's existing Claude `Stop` sensor **before and independent of** notification work (sensor relays observations, runtime publishes — push-not-pull preserved, runtime stays hook-free, Codex two-part invariant untouched; **widens ADR-0040 §3 attention charter** to "host-lifecycle sensors feeding allowlisted runtime executors"; **separate released-only publisher floor**, notify floor 0.71.0 untouched per ADR-0043 rule); gated by new `session_capture` config key default `off` via an **explicit narrow ADR-0035 §3 invariant-1 amendment scoped to `publish-session`** (ADR-0040's config-key shape was notify-scoped, so 0044 carries its own authorization + enumerated own-lock/temp deletion grant; ADR-0041-grade env-only gating rejected as disproportionate for non-network repo-local M1); **slot not ledger** under `state/runtime/session-capture/` — a **narrow ADR-0031 §6 second-artifact amendment** — with `entry.json` as **commit record** (per-file temp+rename, fingerprint-pair mismatch forces republish: mixed generations self-heal, never load-bearing), fingerprint no-op read from the committed entry (includes session_id + note hash), real `O_EXCL` mutex (claimDedupe = precedent not mechanism), and **evidence-not-suppression** (`repo_recent_terminal_evidence` recorded as arbitration data — projection has no session identity, so publisher-side in-workflow suppression is rejected as unsound); staged note = repo-global by design with staging git-context + content hash + 24h fold window + `--clear`, `--file` hardened (no-follow/regular-file/size cap) atop canonicalized write-root containment (lexical-only pointer checks insufficient for an automatic writer); sanitized `entry.json` projection (enumerated single-line clamped fields, `note_staged_at` not frozen age, **no imperative/next_action field** per SessionStart injection ruling, summary presented as untrusted quoted data) as ADR-0045's input contract — S5 owns exit-side production/lifecycle, S6 owns entry arbitration; **explicit v1 scope decision: Claude limb only** (no Codex firing point; shared-fs participation; ADR-0041 cited as shape not equivalence), PreCompact acknowledged as recorded host truth but rejected v1 on ≤1-turn marginal value, transcript mining rejected; hook-grade output discipline (exit 0/stdout-silent, diverging from operator subcommands), conditional-guarantee honesty (safe mode/floors surfaced via settings+doctor readiness diagnosis), durable advisory consumption (no delete-on-read; consumed-markers are ADR-0045-owned siblings), normative schemas/caps/TTLs in plugin-packaged content-token-enforced `session-capture-contract.md` (ADR-0046 vehicle), runtime-releases-first → attention-floor-pin rollout with consumer-first rollback + one-shot artifact cleanup; `context.mjs` gains `publish-session`+`note`+`status --slot` through the ADR-0035 §5 add-gate with mutation-verified hostile-path tests | Accepted |
| [0045](0045-entry-time-proposal-surfaces.md) | Entry-time proposal surfaces — **one arbitrated, pointer-only entry brief**: a single runtime-owned R0 arbiter (`runtime:context entry-brief`) computes all arbitration and at most one marker-paired line reaches the session — never a hook chorus (multi-hook conflicts rejected) and never prose precedence rules (arbitration-by-prose = zero-active-triggers restated); **four explicit scoped governance moves** (silence rejected): ADR-0031 Alternative-A reader boundary narrowly amended for this one observer (versioned tolerant parser over persona/macro state, new code not reader-reuse, degrade-to-`indeterminate` never guess; handoff seam untouched; Approach C = recorded escalation), ADR-0040 sensor-stdout invariant gains a scoped exception (the entry sensor's one line IS its output channel; notify sensors stay silent), ADR-0035 §4 determination recorded (hook-stdout context contribution ≠ enumerated session-lifecycle mutation; arbiter R0, first entry-side write ⇒ M1 §5 add-gate), ADR-0044 §7 amended to conditional presentation (brief omits `summary_line` entirely — stricter; pointer targets validated `entry.json` file, never the capture dir); **pointer-only schema** `runtime-entry-brief-1.0` (closed enums / per-family-validated ids (subtask ids never emitted, counts only) / ages / counts / arbiter-derived pointers — **zero stored free text including `phase`**, all free-text validation gaps closed) with commands **synthesized only from the normative in-ADR state→command table** (never replayed from `routing_recommendation`/`next_action`; localization extracted to a shared leaf module — footer↔context cycle avoided — with explicit trusted-host threading, $-prefixed on Codex); **total-order precedence lattice** with honest refusals: linked child (both-direction validated) leads > sole current-branch workflow > bridged macro readiness (four outcomes; `orchestrator:next` dirty-gated; `in_progress_or_blocked` = aggregate, no command) > branch-matched fresh `entry.json`; **branchless sources (handoff slots incl. `pending|surfaced` marker matrix, context ledger, consensus) are row-only, never lead**; multi-active peers ⇒ `owner-choice-required` (**mtime-recency leader rejected** — Stop snapshots rewrite every active workflow per turn, mtime measures hook scheduling not activity); corruption/ambiguity/overflow above the would-be leader ⇒ `indeterminate`, command suppressed (uniform fail-open rejected); detached HEAD = report-only; dispositions `lead|owner-choice-required(new vocabulary, declared)|no-branch-context|indeterminate`, empty-state hook emission governed by decided sub-key `entry_brief_empty` default silent; surfaces = attention SessionStart `startup` sensor (**widened probe-matrix gate** — matcher vocabulary+double-fire+ordering+safe-mode+stdout semantics recorded in baseline before registration, hook stays unshipped if disproven; new capability-specific discovery + stdout-capturing dispatcher, dedicated released-only floor) + dashboard Tier-1 entry advisory (**snapshot only, excluded from `--watch`**; designer inclusion = deliberate per-surface decision, ADR-0043 demand-gate untouched) + direct CLI both hosts — **gate binds the hook path only** (`--surface`; CLI/dashboard always computable as explicit operator reads); **activation user-scope-only** (`entry_brief` default `off`, tracked repo value cannot enable — closes the repo→user-precedence cloned-repo hole; notify/session_capture repo-key shape deliberately not imported: model-context shaping ≠ artifact writes); UserPromptSubmit deferred w/ evidence triggers; persona compact hooks + one-shot consumption untouched (concurrent-consumption row = accepted stale observation); rollout probe→ADR-0044-implementation→runtime-release→attention-floor-pin with consumer-first ordered rollback; mutation-verified tests incl. command-synthesis isolation (hostile text planted in every stored field incl. phase/paths/ids/timestamps must never reach the brief), indeterminate suppression, gate binding per surface, dispatcher no-op on absent executor | Accepted |
| [0046](0046-machine-bootstrap.md) | Machine bootstrap — first-class `runtime:bootstrap` lifecycle command (subcommand nouns `plan\|status\|resume\|verify` + `profile export\|seed`), **not** a `runtime:settings` flag and **not** doc-only. Two-stage cold start: Stage 0 (host-native marketplace + runtime install) is document-only and **irreducible** — Claude's marketplace-add is not even in the H2 allowlist, and ADR-0006 already rejected a repo-level installer; Stage 1+ is a runtime-conducted **hybrid** interview (diagnose → profile-seeded default → ask → render fragment → **operator applies** → re-probe), ADR-0024 having already rejected doc-only as an end state. **Machine-scoped by construction** (repo-scoped catalog/source readers forbidden) — which is the fix for `runtime:settings` emitting sixteen false `Add <name> to marketplace.json` remediations and *silently* never recommending updates outside the source tree; the consumer-repo misbehavior is a **code defect in one branch** of `runtime:settings`, not a falsified ADR-0024 premise (ADR-0024 defines repo-local vs user-global paths generically) — repairing that branch is a prerequisite, pinned by its own regression. Authorizes one new **machine-global M1 artifact home** (`~/.agentic-plugins/runs/bootstrap/`, `~/.agentic-plugins/profiles/`) as an M1 *location* extension — no new tier, no network, and **no second executor** (H2 plugin-management stays behind `settings --execute-plugin-management`). Separating plan from execute does **not** by itself repair the executor-runs-before-artifact ordering — **write-ahead** does (persist planned intent + plan hash before any action), which narrowly supersedes `settings-report-contract.md`'s "re-run, not rollback" ordering for the plugin-management and cleanup executors. **Secrets-free portable machine profile** = an *untrusted source of interview defaults*, **never an input to any activation or config loader** (three guards: fail-closed secret scrub + boundary validator + static loader test) — generalizes the egress-launcher chat-id pre-fill from one value to the whole machine, credential stays env-only. Completion has two **terminal** states (`complete` requires the bounded cross-host `deep-peer-smoke` proof; otherwise `configured-not-verified`) plus the non-terminal `incomplete`; a one-host machine cannot reach `complete` and is told so rather than looped. ADR carries policy; the **plugin-packaged** `machine-bootstrap-contract.md` (content-token enforced à la `footer-contract.md`, **not** citation-only à la `settings-report-contract.md`) carries the schemas — because only the packaged doc is readable from a consumer machine (`compat.mjs` PLUGIN_ROOT precedent). Nine-axis decide + independent Codex Brainstorm converged (`agreed`, aggregate 2.56/1.56/0.89, sensitivity `flipped: false`); a Codex Plan-verify pass then returned 7 blockers + 12 factual errors against the first draft, all folded in (machine-probe seam, marketplace probe, plugin-set/version policy, planner purity, **write-ahead** durability, declinable set, proof transport) | Accepted |
| [0047](0047-notify-attention-gating-gc.md) | Response-needed notification signal + bounded notify/artifact GC — **new kind `response-needed`** (not a status refinement: `notify_kinds` filters exact kinds only; joins `KINDS_WITH_DEFAULT_STATUS`, subject `session:<id>:<prompt>`, normal urgency) with a Claude **structural interim/final Stop classifier** (2.1.145 `background_tasks`/`session_crons` payload enumeration first — present-but-malformed = unobservable, never "empty"; bounded **dual-home** 4-persona peer-run-ledger liveness fallback with per-status rules (`running|cancel_requested` = PID kill-0, `queued|spawning` = ledger stale-grace freshness) and **no fingerprint spawn** (PID reuse errs interim); **incomplete scan blocks promotion** — cap/timeout/unreadable/malformed/dual-home-ambiguity all degrade to today's bare `turn-complete`; repo-scoped correlation limit stated honestly; transcript semantics re-rejected per ADR-0044 Alt E) and **exactly-one-signal-class precedence** (fresh-projection workflow-terminal batch > `response-needed` > interim `turn-complete`; producer-level no-duplicate rule — dedupe can't enforce it across kinds); realizes the two dead ADR-0041 §3a vocabulary tokens — `your-turn` (final verdict only) + `needs-approval` (`permission_prompt` path) — via a **named narrow §3a producer-domain amendment** (vocabulary/guards/opt-in/Claude-limb scope unchanged); Codex limb: shuttle **remaps `agent-turn-complete` → `response-needed`** as an explicitly **accepted approximation** (soft-degrade: un-re-rendered shuttle keeps emitting still-valid `turn-complete`), approval variant stays silent no-op behind a **seeded standing `runtime:compat` watch row** (evaluated every plan run, not drift-triggered — covers Codex `notify=` variants + the already-recorded Claude 2.1.198 `agent_needs_input`/`agent_completed`; source-verified payload required before any mapping); repairs the **unreachable-EEXIST `withReclaimLock` defect** (`mkdirSync recursive:true` never contends — mutual exclusion restored non-recursive, mirror sweep required) and implements ADR-0040 §2's authorized-but-never-built retention: **bounded fair expired-claim sweep** on the emit path after channel+kinds gates with a pinned sub-order (current-event claim first, current key excluded) that **unlinks only under the claim's own `.reclaim.lock`** (one lock protocol for reclaim/finalize/sweep — lockless sweep re-opens the double-fire window), shared `expired`-vs-`gc_eligible` predicates with the dashboard, rotating-cursor fairness, per-entry failure isolation, and a named self-healing pre-repair transition window + dashboard concurrent-observer unification (readdir→stat `ENOENT` = concurrent change, not `blocked`); **citation-aware runs/ retention** as read-only planner + explicit M1 apply executor (§5 add-gate): closed v1 family registry `doctor|compat|settings` (consensus/context/cutover/machine/image/unknown excluded by name; validated run-id dirs only), 4-pin taxonomy (tracked-doc run-id/path citations incl. bare tokens; `latest.json`; **live/reader-selected runs — non-vacuous**: non-terminal settings executions, older attestation/proof selections; cross-artifact refs = `doctor.json` report snapshots + cutover evidence pointers), **`scan_complete` fail-closed on every pin source**, `pinned_overage` informational vs actionable excess (pins alone over cap ⇒ nothing deletable), **plan-hash binding** (apply recomputes under the family lock and refuses drift — ADR-0046 presented-plan contract), writer coordination via minimum-age guard + last-instant lstat concede, containment + no-follow re-validated at the destructive boundary (TOCTOU), **write-ahead receipts with a per-target `planned→started→completed|failed` state machine** (atomic transitions; open receipt blocks new applies and deletion-capable downgrades; receipts live under `state/`, outside candidate scope), per-invocation ceilings, explicit irreversible delete (quarantine rejected — unbounded growth one directory over); §4 ceiling text untouched (enumerated ADR-gated exemption model); rollout as **two releases mapped to the macro graph** (A = contract+compat-watch → attention floor pin; B = GC, inside acceptance) with a **dual-kind filter window** (`turn-complete,response-needed` both enabled through migration; wrong token/runtime order fail-closes the whole pipeline via `parseKindsFilter` hard-error; both host caches verified — shuttle resolves Codex cache without fallback; rollback = reverse with window held open, shuttle re-rendered from the downgraded package, retention deletions explicitly outside rollback) and a **dedicated released-runtime capability floor** for the classifier (notify floor 0.71.0 untouched — two gates never share a constant) | Accepted |
| [0048](0048-bootstrap-observability.md) | Bootstrap observability — decision gate of the bootstrap/statusline/observability macro. **Stage 5 renamed "operator observability + egress"** and gains per-host declinable `statusline.<host>.configured` steps (no renumbering; Claude step pinned to "canonical configuration observed" — configured≠active per the workspace-trust/`disableAllHooks`/safe-mode gates in `docs/assurance/statusline-host-truth-2026-07-22.md`; terminal 1.1 runs stay immutable, open runs migrate additively). **Shipped-component policy**: native-first — Codex always the native `[tui].status_line` ordered-array fragment (closed upstream vocabulary, external commands an upstream non-goal); Claude behind a 5-condition inline-sufficiency gate, falling back to a **rendered, never auto-installed** credential-free shim for operator installation; one canonical ordered policy definition feeds both renderers; ADR-0041 §6 narrowed to "no *automatically installed* statusline component" and ADR-0040's Tier-4 deferral partially superseded. **Adopted item set (owner-directed): six items** — model-with-reasoning · git-branch · pull-request-number · context-used · five-hour-limit · weekly-limit — version items excluded; profile carries a scalar preset id (ADR-0046 §7 amendment). **Egress evidence vocabulary** for run schema 1.2: kind-discriminated `proof.egress-provider-ack` (evidence member `provider_ack`, `directions` forbidden; fail-closed per-kind validators, no `oneOf`) vs owner claim `completion.egress_receipt_attestation` (`attested` never `passed`, `surface:"owner-phone"`, attempt hash + linked provider-proof hash); combined presentation label `delivery-attested` only — generic `complete` unchanged; freshness binds versions + sanitized activation fingerprint + attempt hash + artifact hash; mandates repair of optional proof validation, unvalidated read boundaries, and last-wins duplicate reduction. **Credential boundary stated honestly**: component-scoped env-only data-flow/persistence boundary (named activation checker + pinned emitter only; executed-proof env inheritance a documented exception; "never supplied to the model"; static named-reader allowlist test; profile `credential_env_var` becomes a schema constant; statusline renderers must not read it) — the literal "tools never see it" wording refuted against the verified reader/emitter/doctor code paths. Nine-axis major decide + independent Codex Brainstorm converged (`agreed`, sensitivity `flipped:false` on all four items); owner approved all four in-session | Accepted |
| [0049](0049-evidence-as-data.md) | Evidence as data — **forward-only**: from the first release loop after the schema lands, each evidence loop gets one closed-schema JSON record under `docs/assurance/evidence/`, and the prose stays hand-written. Motivated by the measured split from five adversarial review rounds on [#644](https://github.com/each4all/agentic-plugins/pull/644): identifier comparisons converged and caught three real defects (a mis-paired `#521`/`v0.77.1` triple, two dangling citations of a squash-deleted branch commit), while prose parsing did not — id/date proximity is undecidable on this corpus (agreeing and disagreeing distances interleave; the #644 distance list is a historical snapshot) and sha→version attribution failed three times in three directions before being removed for attributing 0 of 97. Three measured constraints shape the decision: the record unit is the **evidence loop, not the release** (17 cited tags vs 27 cited run ids; 2026-07-20 alone carries four records, while 0.86.0/0.86.1/0.86.2 collapse into one ADR-0048 loop), the supersession relation is **per-site, not per-record** (the four evidence regions carry different chains and R4 deliberately trails a loop), and three of the five prose sites are **single-line markdown table cells** pinned by `cutover-audit.mjs` + a five-cell row test added after R3 once wrapped and dropped out of the live audit. Every field declares provenance — `derived` (git/manifest, gated everywhere), `observed` (doctor artifact + content hash, **not** gated in CI since `.agentic-plugins/runs/` is gitignored and retention-pruned), `operator-attested`, `authored`; `relations[]` is typed and plural (`follows`/`supersedes`/`amends`/`restores`/`absorbs`). Rendering, generated regions, and migration of the 27 historical records are **deferred to a follow-up ADR**, gated on a typed exporter spike (the shipped extractors return counts, not records, and extract→render→extract is circular without an independent coverage manifest) and a resolution for the single-line table constraint. Honest headline cost: on landing this **adds** a sixth copy and does not remove the recurring recovery PR — withdrawal trigger recorded if the follow-up does not open within two release loops | Accepted |
| [0050](0050-fragment-persistence-boundary.md) | Fragment persistence boundary — **immutable, content-addressed fragments; the manifest pointer is the only mutable thing; the manifest write stays the sole commit point**. Four follow-up rows sit on this boundary and three explicitly defer to it (the G7 freeze row, §7 invalidation outside the fixpoint, unpinned fragment-composition readers, and the notify `apply_command` pointing at a stripped artifact). The recorded framing assumed a **CAS/transaction was missing**, after a round-5 restore-rewrite was withdrawn in round 6 because "the fragment write and the manifest's authority withdrawal have no CAS transaction ordering them". Measurement reversed that premise: `composeFragments` (atomic file writes, unlocked) already runs before `updateBootstrapRun` (atomic, family-locked), which is **write-ahead ordering with the manifest as the sole commit point** — reproduced by clearing every pointer from a planned run's manifest while leaving the files, where `resume` restored all 5 pointers and self-healed. Creation is therefore already crash-safe; the real defect is that fragments are **name-keyed** (`<name>.fragment`), so a re-render or restore overwrites a file a live pointer already references — expressing a change as a mutation rather than a creation. Decision: address fragments by content (`<name>-<sha256[:12]>.fragment`, using the `sha256` `writeBootstrapFragment` already computes and returns), make a re-render a create-and-repoint, and reduce the freeze to a **pointer** policy recorded under the lock that is already atomic — removing byte mutation under a live pointer without new machinery. **Cross-host Plan-verify then refuted three supporting claims and they are corrected in place**: `writeFileAtomic` has no `fsync`, so the two renames are unordered under POWER failure and only process death is covered; a manifest CAS is still required and the contract already declares that gap (§2105, "no cross-process CAS … one resume at a time"), so "a new manifest pointing at an older frozen fragment" stays representable under concurrency; and a content hash identifies OUTPUT bytes, not the input snapshot, so the unpinned-readers row is not addressed at all. The three entangled rows are therefore **not resolved** — one storage hazard is removed from under them. Rejected: CAS-over-both (builds machinery to make an unnecessary in-place mutation safe, and a second commit protocol can disagree with the manifest), status-quo warnings, and version-bump-only invalidation (a trigger, not a model). Costs stated: fragment paths stop being reconstructable from `<name>`, superseded fragments have NO owner (retention covers `doctor|compat|settings`, is repo-local, and prunes whole runs), and the deferred migration audit found real production breakage (two readers open `notification-plan.fragment` by name; `statusline-shim` keeps no pointer at all). **When** a re-render should fire is deliberately left to the §7 row, which this unblocks | Accepted |
| [0051](0051-host-parity-baseline-source.md) | Host-parity baseline source — **the packaged copy is the sole runtime authority, and changing the baseline obliges a runtime release**. Four readers disagreed (`compat.mjs:790` = `PLUGIN_ROOT`; `doctor.mjs:1159`, `dashboard.mjs:403` and the CI-only `scripts/check-host-version-drift.mjs:33` = repo root), and the split was assumed latent until measured 2026-08-09 — where it turned out to be **three-way, not two**: repository and the **Codex** cache of `0.89.0` both read `2026-08-08 / 2.1.226 / 0.147.0` while the **Claude** cache of the same `0.89.0` read `2026-07-25 / 2.1.220 / 0.145.0`, because `compat` reads `PLUGIN_ROOT`, so the same command answers differently per host on one machine at one commit — "the packaged copy" was not one artifact. Layered on that, the baseline is refreshed by the **post-release** recovery loop (`16b1833`), so a refresh always lands after the release that would have carried it. **Two claims in the first draft were refuted by cross-host review and are corrected in place**: "over-reporting, never silence" is false — `compat.mjs:397` compares with exact inequality, not ordering, so a host pinned or rolled back to `2.1.220`/`0.145.0` makes the stale package report `matches` (silence) while the fresh baseline reports `version_changed`; and re-ingestion **does** clear `release_notes_required` (measured: 495,499 B + 307,141 B moved the run to `gap_analysis_ready`) — what persists is the drift itself, holding `runtime_handoff_artifacts` at `partial` 9/15, the observed 95% `experience_parity` ceiling. Root cause is a conflict between accepted documents, not a code slip: ADR-0026 §3 resolves a gap through **either** content-backed notes **or** a repository-reviewed refresh (the draft misquoted this as refresh-only), while `machine-bootstrap-contract.md` §preamble / ADR-0046:444 name `PLUGIN_ROOT` the precedent and `repoRoot` the anti-pattern. Decision: one authority (`PLUGIN_ROOT`) with the CI drift script explicitly carved out; **a release obligation as the precondition** — the pin was withdrawn during implementation once measurement showed both catalogs resolve the same local path, a `local`-source Codex entry carries no top-level version by design, and the split needed two factors (content moving under a fixed version **and** one host whose update path re-copies regardless); the dated `Observed on` header as canonical grammar with **visible failure instead of silent degrade** (safe precisely because there is no fallback); content-SHA-256 provenance because origin+path is not audit-grade (`PLUGIN_ROOT` can be a dev checkout); convergence promised only for fresh reads, since `compat` binds the baseline into its snapshot (`:93`, reused at `:140`/`:314`) and must intentionally disagree with a live doctor read; and a formal amendment promoting the illustrative locality statement to a normative rule for baseline-class assets. Costs stated plainly: a refresh has **no effect until a release**, so the 95% ceiling persists through that window by design, and release cadence couples to host-release cadence. Rejected: **A** package-only without the release obligation (refresh silently inert), **B** repo-root everywhere (`missing` for every consumer), **C** repository-override precedence — **adopted in a first draft and withdrawn**: `repoRoot` is the current Git top-level (`commands/compat.md:13`), so any fork, stale branch, or dirty worktree becomes an authority, and the draft blessed uncommitted edits while claiming ADR-0026's reviewed-acceptance rule was intact, **D** relocate to a runtime-owned artifact — the first draft's rejection (that it forfeits PR review) was a **false coupling**, corrected here: storage and approval separate cleanly via a content-addressed candidate plus a tracked acceptance record, so D is deferred on scope only and named the successor if the release obligation proves too costly | Accepted |
| [0052](0052-release-obligation-enforcement.md) | Release-obligation enforcement — **a history-and-tag reconciliation check, not a PR-time metadata gate; ADR-0051 §Alternatives D stays deferred with its trigger measured rather than assumed**. ADR-0051 §Out of scope left §Decision 2 unenforced and named `16b1833` the counterexample. Measurement first reframed the counterexample: its baseline blob is **byte-identical to the blob at tag `plugin-runtime-v0.90.0`**, so the change did ship — 54.0h later, riding an unrelated `feat`, while the release PR in between (`fd7ab8e`) left runtime at `0.89.0`. The defect is a **54h divergence window closed by luck**, the second-worst of the 38 in project history (median 5.3h, 9 over 24h, 5 over 48h). The one full obligation exercised end to end cost **51.84h**, and it decomposes into two obligations that are not the same one: ADR-0051's release costs **0.13h — 0.3%** (median **4m48s** across all 163 release commits), while `sync-doc-versions.mjs:306`'s proof-coupled doc gate costs **~23.09h** on any runtime bump, and 28.61h of detection+authoring+review is present under **every** design. **D is re-judged and deferred**: both clauses of ADR-0051's own trigger measure negative — the release is not costly, and the baseline changes faster than *reviewed acceptance* can land, which D keeps; granting D everything downstream of the refresh merge still clears only 3/12 host-publish gaps (18.8% duty vs the measured 18.4%), so **D buys no convergence**. D's shadow-proof pointer question is nonetheless answered in §Alternatives (packaged trust anchor + fetched, hash-verified acceptance record + machine-global immutable object and single repointed ref, the ADR-0050 create-and-repoint pattern) so the deferral is honest and re-triggerable. Decision: protect `docs/host-parity-baseline.md`, `data/plugin-set.json` and `data/schemas/**` **by directory, not enumeration** (an enumerated list drifts — `EVIDENCE_DOCS` is the live instance — and the directory settles `runtime-plugin-set-1.0.json`, registered but today reached only by tests); read the two plugin manifests as promotion **evidence, never triggers**, since treating their bump as a new obligation recurses; require a reachable tag that advances the manifest **and** carries the accumulated protected tree; **fail closed on shallow history or missing tags**, require an **adoption epoch** (without one the check condemns 24/38 historical changes on first run), and re-run the post-tag assertion **inside `release-please.yml`** because a `GITHUB_TOKEN` push triggers no workflow — all three already proven by the sibling `check-doc-evidence.mjs` gate. Rejected: **A** routing-type enforcement at PR time — measured to reject **24/38 (63%)** of historical baseline changes, i.e. the dominant authoring form, proves intent rather than delivery, and is bypassable because every workflow uses a bare `pull_request:` whose default types exclude `edited` while the squash subject comes from the title; **B** tag-consumed obligation ledger — a real state machine (coalescing, supersession, in-flight, tag races) that deadlocks if a pending check also blocks the release PR, and cannot record its own future squash sha; **C′** a cron watchdog with a staleness threshold — **prototyped and disproved**: simulated over the real twice-daily cadence a 48h threshold **misses `16b1833`**, whose over-threshold interval is only 6.0h wide and contains no firing, so the threshold is coupled to schedule granularity rather than free; **F** a `sync-companion-bundles.mjs`-shaped content lock — ill-formed here, since the divergence was between installed caches and the repository, which no in-tree comparison can see. `main` carries **no branch protection** (`404 Branch not protected`), so a PR-time gate could not have blocked the merge either — the honest comparison is two detectors with equal power and unequal cost. Three follow-ups recorded, not folded in: proof-coupling scope (the largest cost lever measured), patch-tolerance alignment (CI calls a host patch informational for 14 days while `doctor:1184`/`compat:405` call it immediately stale — and a patch bump is what triggered the 51.84h loop), and cross-package scope for attention's `runtime-floors.json` plus the rendered-receiver second tier | Accepted |
| [0053](0053-baseline-exactness-and-compatibility-assurance.md) | The baseline has **no assurance verdict** — strict normalized equality is retained unchanged as *exactness*, a human-granted *compatibility assurance* fact is added, and an *integrity* layer outranks both. The question arrived as a treadmill ("is strict equality too strict?") and measurement inverted it twice. First on the numbers: `doctor` compares the baseline against the **locally installed** host, not the newest published one, so ADR-0052's 18.4% duty cycle answers a different gate — across 70 recorded proofs over 46.7 days the verdict is `current` in **42/70 runs (60.0%)**; semver-position grading would silence **17 of 18 Claude Code steps** (the product has been `2.1.x` throughout) including the one lap that produced real adoption work (`2.1.233`'s `TaskCreate`/`TodoWrite` withdrawal, a **patch** bump); the companion 14-day age window has **never fired** (0/18); and a closed window cannot cover the only direction the host moves. Then on the framing: cross-host review disproved the draft's "`current` is a false assurance" claim — the check is *labelled* baseline freshness and evidences exactly what it compared — so the defect is that **no assurance fact exists** and `cutover-audit.mjs:112` promotes an exactness fact into readiness. A missing fact cannot be reworded, which is what defeats Alternatives A and C-min. Review also found that moving `CHECK_PASS` membership is **not sufficient**: exactness reaches cutover a second time through `compat`'s exact-equality `drift_class`, so §Decision 4 moves both paths. Integrity states, grant-vs-match, residual-tolerant grants, package binding, cohort non-Cartesian-ness, `same-precedence-nonexact` direction and the migration/precedence rules are decided here rather than deferred, because each is a fail-closed property. Dated-header grammar unchanged; assurance grammar **added and single-sourced** in the same module. Amends ADR-0051 (packaged-baseline section; authority, release obligation and single-sourcing untouched) and **closes ADR-0052 §Decision 6's patch-tolerance-alignment follow-up as rejected on measurement**, keeping only its direction-aware half. Decision only; schema, doctor schema-version bump and code split to a follow-up | Accepted |
